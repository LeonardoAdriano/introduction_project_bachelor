<%= form_with(model: course) do |form| %>
  <% if course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(course.errors.count, "error") %> prohibited this course from being saved:</h2>

      <ul>
        <% course.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :private %>
    <%= form.check_box :private %>
  </div>

  <div class="field">
    <%= form.label :valid_until %>
    <%= form.date_select :valid_until %>
  </div>

  <div class="field">
    <%= form.label :accessible_until %>
    <%= form.date_select :accessible_until %>
  </div>

  <div class="field">
    <%= form.label :accessible_from %>
    <%= form.date_select :accessible_from %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
